# URL base da API (só necessário se não usar o proxy do Vite)
# Em desenvolvimento, o proxy do vite.config.js já redireciona /api → localhost:9002
VITE_API_BASE_URL=http://localhost:9002

# URL pública do app (usada em links de cancelamento, etc.)
VITE_APP_URL=http://localhost:3000
